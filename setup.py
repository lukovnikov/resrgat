from setuptools import setup, find_packages

setup(name="ldgnn",
      description="ldgnn",
      author="Sum-Ting Wong",
      author_email="sumting@wo.ng",
      install_requires=[],
      packages=["ldgnn"],
      )
